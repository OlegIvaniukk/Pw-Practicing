# Playwright Test Automation — AutomationExercise

This project contains automated **End-to-End UI tests** for the **AutomationExercise** platform, implemented using **Playwright** in JavaScript/TypeScript.

## Project Goal
To ensure stability of core user flows by automating **UI regression testing**, applying modern test-automation tools and best QA engineering practices.

## Implemented Capabilities
- Automation of key E2E UI scenarios
- Playwright Test Runner + built-in assertions
- iframe testing support (if required)
- CI-ready execution (GitHub Actions compatible)
- Test reports and local execution artifacts

## Tech Stack
- Playwright
- JavaScript / TypeScript
- Node.js

## Installation
```bash
git clone <your-repo-url>
cd <project-folder>
npm install
